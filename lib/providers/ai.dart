class Ai {
  static late Ai instance;

  fn1(){
    //adding new lang
    //send the default lang as json and get the new lang 
  }

  fn2(){
    //adding new key

    // send the key or the default lang and get the tr for all the fields in all langs

    //  final model = GenerativeModel(model: "gemini-1.5-flash", apiKey: apiKey);

    //     final response = await model.generateContent([
    //       Content.text("Explain how AI works"),
    //     ]);
  }
  

}